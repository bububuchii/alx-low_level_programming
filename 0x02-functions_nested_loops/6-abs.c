#include "main.h"

/**
 * _abs - gives the absolute value
 * of an integer.
 *
 * @n: input number as an integer.
 *
 * Return: absolute value
 */
int _abs(int n)
{
	int a;

	if (n > 0)
		a = (n * -1);
	else
		a = n;
	return (0);
}
