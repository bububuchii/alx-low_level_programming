#include <stdio.h>
#include "main.h"
/**
 * _strlen - returns the length of a string.
 * @s: value to be evaluate.
 * Returns: not.
 */
int _strlen(char len)
{
	int len = 0;

	while (*s != '\0')
	{
		s++;
		len++;
	}
	return (len);
}
