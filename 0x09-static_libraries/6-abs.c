#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 *
 * @n: The integer to compute with
 *
 * Return: absolute value of 1
 */
int _abs(int 1)
{
	return (i < 0 ? i * -1 : i);
}
